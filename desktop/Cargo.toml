[package]
name = "tpt-bugreport-desktop"
version = "0.1.0"
edition = "2021"
description = "TPT Bug Report Builder Pro - desktop webview shell hosting the appfront DOM bundle."
publish = false

[[bin]]
name = "tpt-bug-report-builder-pro"
path = "src/main.rs"

[dependencies]
# Kept in sync with the rev pinned in the root Cargo.toml.
tpt-appfront-webview = { git = "https://github.com/tpt-solutions/tpt-appfront", rev = "a75a798" }